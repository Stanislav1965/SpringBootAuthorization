
        location / {
            root   html;
            index  index.html index.htm;
        }
        # >= добавил в конфигурационный файл
        # path к html-форме авторизации  html/signin/sign.html;       
        location /signin/ {
            index sign.html;
        }
        # <= добавил в конфигурационный файл           
   
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # >= добавил в конфигурационный файл        
        location ~ \.php$ {
            proxy_pass   http://127.0.0.1:8080;
        }
        # <= добавил в конфигурационный файл  